/**name: J.P. Pacheaco
Assignment: Lab 1
*/

//this "program" file is the main file

#include <stdio.h>

int main () //use int if we are returning an int
{
    //get input from user
    char input [];
    cout << "Enter the number you'd like to convert: " << input;

    int size = sizeof(input);
    int output [size];

    for(int i = 0; i<size; ++i){
        int var = (int)(input[i]);
        //convert
        //add to output
    }

    //merge output array
    //output final binary code

}